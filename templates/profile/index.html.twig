{% extends 'base.html.twig' %}

{% block title %}Gestion Profile
{% endblock %}

{% block body %}
	<h1>Gestion Profile</h1>
	{{ form_start(form) }}
	{{ form_row(form.name) }}
	{{ form_row(form.email) }}
	<button type="submit" class="btn">Register</button>
	{{ form_end(form) }}
{% endblock %}
